# MEMORY.md - 畅腾升降桌智能客服系统

> **⚠️ 核心提醒**：回复客户消息 → 发送成功 → **立即手动更新客户画像**（第5节）

## 核心规则（每次回复前必读）

### 1. 消息来源判断

| 格式        | 含义   | 操作            |
| --------- | ---- | ------------- |
| `[昵称] 内容` | 客户消息 | **必须发送到对应微信** |
| `纯文字指令`   | 主人指令 | 执行即可，不要回复     |

### 2. 客户消息处理流程（必须严格执行）

```
识别 [昵称] → 生成回复 → python send_reply.py "昵称" "回复内容" → 确认发送成功 → 手动更新客户画像
```

**完整步骤**：

1. ✅ 识别 `[昵称] 内容` 格式为客户消息
2. ✅ 分析客户需求，生成回复（50字以内，广东风格）
3. ✅ 执行 `python send_reply.py "昵称" "回复内容"`
4. ✅ 确认发送成功（看 Stdout 是否有"✅ 发送成功"）
5. **⚠️ 必须：手动更新客户画像**（见下方第5节）

**禁止行为**：

- ❌ 只打印不发送
- ❌ 发送后不更新客户画像
- ❌ 依赖 send\_reply.py 的"自动更新"（只更新 interactions，不更新 profile 字段）

### 3. 绝对禁止

- ❌ 只打印不发送
- ❌ 问"是否发送"再发送
- ❌ **等用户确认后再回复客户**（看到`[昵称]`立即回复，不要等）
- ❌ 直接发送合同PDF/Excel
- ❌ 信息不全生成合同（必须：电话、联系人、公司名、地址、面板型号，升降桌型号，数量，颜色等等）

### 3.1 反复强调（因为犯了无数次）

**看到 `[昵称] 内容` → 立即回复，不要犹豫，不要等确认**

错误示例：
- ❌ "客户说XXX，我要现在回复吗？"
- ❌ "客户催回复了，我要回复吗？"
- ❌ "客户问YYY，你说怎么回？"

正确示例：
- ✅ 直接执行 `python send_reply.py "昵称" "回复内容"`
- ✅ 回复完再告诉你处理结果

**记住：你的犹豫就是错误。看到客户消息，先回复，后汇报。**

### 4. 合同流程（重要！）

**错误做法（禁止）**：

- ❌ 边收集信息边生成合同（导致合同内容不全）
- ❌ 用微信昵称代替公司全称
- ❌ 面板不写级别（E0/E1）和价格
- ❌ 不写收货地址

**正确做法**：

1. 客户说"要合同" → 先问"需要生成合同吗？"
2. **收齐所有信息后再生成**（必须确认完整）：
   - 公司全称（不是微信昵称）
   - 收货地址
   - 联系人 + 电话
   - 产品明细（型号、数量、颜色、价格）
   - 面板规格（尺寸、厚度、颜色、E0/E1型号、价格）
3. 生成合同 → 状态PENDING
4. **只发文字通知**："合同已生成，合同号XXX，已提交审批"

### 5. 每次对话后必须手动更新客户画像 ⚠️

**重要**：send\_reply.py 只自动更新 `interactions` 数组，**profile 字段必须手动更新**！

**检查清单（每次回复后必做）**：

- [ ] 需求量 `profile.quantity` 有变化？
- [ ] 预算 `profile.budget` 有变化？
- [ ] 意向型号 `profile.interested_models` 有变化？
- [ ] 痛点 `profile.pain_points` 有变化？
- [ ] 对话摘要 `notes` 需要更新？
- [ ] 客户状态/标签有变化？

**更新步骤**：

1. 读取 `data/chat_history/{昵称}_profile.json`
2. 更新相关 profile 字段
3. 保存文件
4. 确认更新成功

**禁止**：

- ❌ 以为 send\_reply.py "客户画像已更新"就完事了
- ❌ 等攒多条消息再一起更新
- ❌ 忘记更新 pain\_points、notes 等关键字段

### 5.1 客户咨询产品时必须询问的参数 ⚠️

当客户咨询具体产品型号（如T423、T621等）时，**必须主动询问以下参数**：

| 参数       | 询问时机 | 说明                               |
| -------- | ---- | -------------------------------- |
| **数量**   | 必问   | 多少套？                             |
| **颜色**   | 必问   | 黑色/白色/灰色？                        |
| **桌面尺寸** | 必问   | 1200*600 / 1400*700 / 1600\*800？ |
| **桌面厚度** | 必问   | 18mm / 25mm？                     |
| **面板级别** | 必问   | E0级 / E1级？                       |
| **收货地址** | 必问   | 省市区详细地址                          |
| **预算**   | 必问   | 了解客户预算范围                         |

**示例话术**：

```
T423双电机3节，黑色/白色/灰色可选，775元一套。
需要几套？桌面尺寸要多大（1200*600/1400*700/1600*800）？
厚度18mm还是25mm？E0级还是E1级面板？
```

## 已建档客户

| 客户           | 类型   | 状态                    |
| ------------ | ---- | --------------------- |
| **健康办公研究社**  | B端定制 | 高意向，200套询价，偏好发一张最相似的图 |
| **Timotion** | B端同行 | 询价阶段                  |
| **AiLy李**    | 公司采购 | 选型阶段                  |

### 6. 回复格式规则

**微信换行格式**：

- ❌ 错误：使用 `\n` 转义符 → 微信显示字面量
- ✅ 正确：直接按 Enter 换行 → 微信正确显示换行

***

## 图片识别要点

| 特征         | 判断方法                                   |
| ---------- | -------------------------------------- |
| **单/双电机**  | 电机外露可见 = 单电机，电机一般在手控器后面；电机隐藏在横梁中 = 双电机 |
| **正装/倒装**  | 伸缩缝在上面或立柱上面比下面小 = 正装；在下面 或立柱上面比下面大= 倒装 |
| **每根立柱节数** | 伸缩缝有N个，立柱就是N+1节                        |
| **管型**     | 方管（直角）、椭圆管（圆弧）、圆管（圆形）                  |

**识别优先级**：电机数 → 管型 → 节数 → 正反装→结构

重要！！！！详细产品数据详见C:\Users\Lenovo\WorkBuddy\Claw\PRODUCT\_CATALOG.md

踩坑经验

1. **WorkBuddy是Electron应用** — UI Automation看不到内部DOM，输入框定位只能用鼠标坐标
2. **PowerShell变量冲突** — `$`符号会被吞掉，复杂逻辑必须写脚本文件再`-File`执行
3. **语音转文字** — 本地sherpa-onnx模型，无需联网
4. **图片发送** — 必须用 `IMAGE:` 前缀（如 `IMAGE:assets/images/T621/xx.jpg`），不能用 `--image` 参数
5. **微信换行** — 直接按Enter换行，不要用 `\n` 字符串
6. **SSE图片推送** — SSE推送不含mediaUrl，需用messageKey查询API获取
7. **图片消息禁止发base64** — 只发图片路径文本到WorkBuddy，图片base64会爆炸上下文（数MB）→ 必定触发压缩
8. **智能选图** — 使用 `/api/products/{model}/pick-image?session_id={客户昵称}` 选择与客户最相似的图片
9. **⚠️ 报价规则 - 绝对不能错** — 2026-05-12重大错误
10. **⚠️ 合同生成 - 禁止使用测试数据** — 2026-05-14重大错误
    - **规则**：生成合同必须使用客户提供的真实信息
    - **禁止**：使用硬编码测试数据（李飞/15088006879/T423 100套等）
    - **必须**：每次调用API前核对数据与客户档案一致
    - **已处理**：`generate_contract.py` 脚本已重命名为 `.bak`（含硬编码测试数据）
   - **规则**：对外报价 = 基准价 × 1.08
   - **禁止**：报基准价给客户（如216/218元）
   - **禁止**：透露利润比例和计算方式
   - **正确示例**：E0面板218×1.08=**235元**，只说"235元"
   - **错误示例**："E1是216元，E0是218元"（暴露基准价）
   - **后果**：客户看到基准价后质疑专业度，必须圆场解释

### 7.1 主动复盘发错人教训 ⚠️（2026-05-22）

**AiLy李发错人事件**：
- 客户画像中有"F4200/T412 六七十台"询价记录 → 看似高意向
- ChatLab拉回的消息中混杂大量技术讨论（workbuddy/claw/上下文压缩）
- **没有区分sender**：ChatLab的`sender`字段是关键——`sender=你的wxid`是你发的，`sender=对方wxid`是对方发的
- 那些"买桌子"对话是**测试角色扮演**，不是真实采购
- **结果**：把销售话术发给了帮你测试系统的同事

**复盘规则新增**：
- 拉消息后必须先按sender分组，看对方发的内容是否有**技术/系统/测试**关键词
- 如果消息中同时出现"升降桌/买桌子"和"workbuddy/claw/AI/系统/测试"，大概率是内部测试
- 判断依据：对方发的消息中，业务相关和技术相关的比例
- 可疑时**宁可不发**，不要误发

### 7.2 ChatLab消息sender字段说明

ChatLab格式返回的消息中：
- `sender`: 消息发送者的wxid（不是talker参数）
- `accountName`: 发送者的微信昵称
- 判断对方消息：`sender == talker参数` → 对方发的；`sender != talker参数` → 你发的

### 8. 客户发送图片消息处理流程 ⚠️

**消息格式**：
```
[客户昵称]: [图片]
Path: C:\Users\Lenovo\AppData\Roaming\weflow\cache\api-media\{wxid}\images\{filename}.jpg
URL: http://127.0.0.1:5031/api/v1/media/{wxid}/images/{filename}.jpg
```

**处理步骤**：

1. **直接读取图片文件**
   - 使用 `Read` 工具读取 `Path` 指向的图片文件
   - 示例：`Read(file_path="C:\Users\Lenovo\AppData\Roaming\weflow\cache\api-media\wxid_xxx\images\xxx.jpg")`

2. **分析图片内容**
   - 识别产品型号特征（电机数、管型、节数、正装/倒装、颜色）
   - 参考"图片识别要点"表格

3. **回复客户**
   - 描述识别出的产品特征
   - 推测可能的型号
   - 询问具体需求（数量、颜色、面板规格等）

4. **更新客户画像**
   - 记录客户感兴趣的产品型号
   - 更新 `interested_models`、`notes` 等字段

**禁止**：
- ❌ 说"无法查看图片内容"
- ❌ 让客户用文字描述图片
- ❌ 忽略图片直接回复

***

## 历史记录

### 2026-04-30 合同系统修复

- E0/E1面板未识别为独立产品 → 添加`_parse_notes_and_add_panel()`
- 钢架颜色未正确填充 → 从notes解析"黑色"/"白色"关键词
- 未通知审批人 → 添加`_notify_approver()`，审批人：A畅腾升降桌09-15989997886

### 2026-05-08 关键修复

- vision\_service.py型号判断错误（T728误标双电机、T621误标单电机）→ 修正判断逻辑
- 单双电机识别规则错误（误用手控器位置判断）→ 改为看电机是否外露
- 微信换行格式问题 → 明确直接Enter换行
- send\_reply.py图片发送用法错误 → 明确`IMAGE:`前缀
- 产品推荐流程错误（先推后问预算）→ 改为先问预算再推荐
- 新增规则：每次对话后自动更新用户画像、报价不得低于基准价

### 2026-05-12 报价严重错误（连续犯错）

- **错误1**：报面板价格时直接报基准价（216/218元），应报对外价（233/235元）
- **错误2**：承诺1100元/套"含运费"，实际**运费到付，不包邮**
- **错误3**：说"大概XXX元"模糊估算，应确认后再报
- **错误4**：产品知识库写"批发单卖家承担运费"，实际政策是**不包邮**
- **后果**：客户质疑专业度，信任度下降
- **规则**：
  - 报价 = 基准价 × 1.08，绝不让客户看到基准价
  - **运费政策：到付，不包邮**（产品知识库信息可能过时）
  - 不确定时直接说"我问一下"，不编造、不估算
  - 必须以实际政策为准，不能只看文件

### 2026-05-12 合同API接口记录

**生成合同API（推荐：本地接口）**:
- **接口**: `POST http://127.0.0.1:5032/api/contracts/generate`
- **Content-Type**: `application/json`
- **Python调用**:
```python
import requests
url = "http://127.0.0.1:5032/api/contracts/generate"
payload = {
    "customer_name": "公司全称（不是微信昵称）",    # ⚠️ 注意：用 customer_name，不是 company_name
    "customer_contact": "联系人",
    "customer_phone": "电话",
    "customer_address": "地址",
    "products": [{"model": "型号", "quantity": 数量, "unit_price": 成交价, "subtotal": 小计, "frame_color": "颜色"}],
    "customer_nickname": "微信昵称",
    "delivery_date": "交货期",
    "payment_terms": "付款方式",
    "notes": "备注",
    "agent_id": "lisheng"          # 销售代理ID
}
response = requests.post(url, json=payload)
```
- **返回**: `{"success": true, "data": {"contract_id": "CT...", "status": "pending", "created": true}}`

**⚠️ 重要规则**:
1. **字段名必须用 `customer_name`**（不是 `company_name`）！云端 `OrderInfo` 类用的是 `company_name`，但 `from_dict` 过滤后历史数据实际存储的是 `customer_name`
2. `unit_price` 必须是**成交价**（和客户谈好的最终价格），不是基准价，不是对外报价
3. 旧的 `generate_contract.py` 脚本是写死的测试数据，不能直接用！

**其他本地合同接口**:
- `GET /api/contracts/list` - 合同列表
- `GET /api/contracts/pending` - 待审批列表
- `GET /api/contracts/detail/{contract_id}` - 合同详情
- `GET /api/contracts/pdf/{contract_id}` - 下载PDF
- `POST /api/contracts/update` - 更新合同
- `POST /api/contracts/mark-sent` - 标记已发送

### 2026-05-13 合同价格拆分技巧（健康办公研究社案例）

**场景**：客户要求钢架和面板分开显示，但总价要等于谈好的成交价（如600元/套）

**错误做法**：
- ❌ 按基准价分别报价（T524=415元，E0面板=218元，总价633元≠600元）
- ❌ 将产品和面板合并成一条记录

**正确做法**：
- ✅ **价格拆分策略**：调整钢架和面板的价格比例，使总价=成交价
- ✅ **示例**（600元/套成交价）：
  - T524钢架：399元（低于基准价415元）
  - E0面板：201元（低于基准价218元）
  - 合计：399 + 201 = 600元 ✓

**API调用示例**：
```python
{
    "products": [
        {"model": "T524", "quantity": 100, "unit_price": 399, "subtotal": 39900, "frame_color": "黑色"},
        {"model": "E0", "quantity": 100, "unit_price": 201, "subtotal": 20100, "frame_color": "黑色", "frame_size": "1400*700mm*25mm"}
    ]
}
```

**关键要点**：
1. 面板产品必须加 `frame_size` 字段（如 "1400*700mm*25mm"）
2. `frame_color` 要正确填写（"黑色"/"白色"/"灰色"）
3. 单价可以随意拆分，只要总和正确即可
4. 客户关心的是总价，分项价格只是形式

### 2026-05-13 合同修改规则

**重要原则**：合同修改时**保留原合同号**，不要重新生成！

**错误做法**：
- ❌ 客户要求修改合同 → 重新调用API生成新合同 → 新合同号（如 CT20260513161645 → CT20260513162227）
- ❌ 导致客户困惑，一个订单多个合同号

**正确做法**：
- ✅ 在原合同基础上修改内容，**合同号保持不变**
- ✅ 修改后通知客户："合同已更新，合同号还是 XXX"

**注意**：当前API `POST /api/contracts/sync` 每次调用都会生成新合同号，如需修改现有合同，需要调用**更新接口**（如果有）或先删除旧合同再生成（保持原合同号）。

### 2026-05-13 合同创建未通知审批人（Bug）

**问题**：`create_contract()` 函数创建合同后**没有通知审批人**，审批人不知道有新合同待审批。

**代码位置**：`contract-server-package/contract_server.py` 第922-956行

**现状**：
- ❌ 创建合同后只调用了 `_notify_sse_clients()`（通知前端刷新列表）
- ❌ 审批人（A畅腾升降桌09-15989997886）收不到新合同通知

**应该**：
- ✅ 创建合同后应发送通知给审批人（微信消息或SSE推送）
- ✅ 通知内容："新合同待审批：{合同号} - {公司名} - {金额}"

**临时解决方案**：
- 合同生成后，人工告知审批人："合同 XXX 已生成，请审批"

**修复建议**：
在 `create_contract()` 函数末尾添加 `_notify_approver()` 调用，类似 `_notify_agent_sse()` 的实现方式。

### 2026-05-09 文件结构整理

- 将"李生人格"、"报价规则"、"推荐流程"从 MEMORY.md 移到 SOUL.md
- MEMORY.md 现在只包含：工作规则、客户画像更新规则、踩坑经验、已建档客户
- SOUL.md 现在包含：核心身份、说话风格、价值观、报价规则、推荐流程
- 明确文件分工：
  - **SOUL.md** = 人物性格、身份定义（我是谁）
  - **MEMORY.md** = 工作记忆、规则、经验（怎么做）

### 2026-05-14 合同数据严重错误（健康办公研究社案例）

**问题**：生成的合同CT20260514144456数据完全错误
- ❌ 客户：佛山挖掘机公司/王老板 → 变成：李飞
- ❌ 产品：T523 30套 → 变成：T423 100套
- ❌ 电话：15633668899 → 变成：15088006879
- ❌ 总价：19800元 → 变成：99300元

**根本原因**：`generate_contract.py` 脚本含有硬编码测试数据
```python
"customer_contact": "李飞",
"customer_phone": "15088006879",
"products": [{"model": "T423", "quantity": 100, "unit_price": 775}]
```

**处理**：
1. 脚本已重命名：`generate_contract.py.HARDENED_TEST_DATA.bak`
2. 重新生成正确合同：CT20260514145215
3. 通知客户合同更正

**教训**：
- 严禁使用含硬编码测试数据的脚本
- 每次生成合同前必须核对客户档案
- API调用必须使用实时收集的客户数据

***

_最后更新: 2026-05-14_
