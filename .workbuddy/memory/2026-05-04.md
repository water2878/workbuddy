# 2026-05-04 工作记录

## 价格更新
- T621 正确价格为 **835元**（之前 MEMORY.md 中记录的 880元有误，已确认以 PRODUCT_CATALOG.md 为准）

## personas/lisheng.md 更新
- 新增"产品推荐策略"章节，明确按场景推荐原则
- 禁止上来就推 T621/F4404，必须先问场景、数量、预算再推荐
- 按场景列出推荐对应型号（写字楼/家用/高端/预算紧张等）

## product_service.py 智能发图功能
- 新增智能选图逻辑：客户指定 → 智能挑选最佳角度 → 顺序发下一张 → 兜底
- `pick_best_image()` 函数增强：支持智能评分（序号图+分、jpg+分、细节图-分）
- 新增 `get_next_image_for_customer()` 便捷接口
- `send_reply.py` 支持 `MODEL:T524` 格式智能发图

## 发图规则重要更新（必须遵守）
- **发图前必须查看图片内容**（用Read工具读取图片）
- 不能盲目按文件名顺序发
- 客户要"正面整体图" → 必须确认是场景图才发，不能发桌脚细节图
- 已更新 `personas/shared/image_rules.md`

## 客户交互
- **健康办公研究社** 要求重新发 T524 照片
- 查看 T523 图片内容：_01 桌脚细节、_02 _03 整体场景图
- 发送 _03 场景图（深色木纹桌面，现代办公场景）
- 备注：T524 目前无独立产品图，使用 T523 场景图替代

## 问题修复：T524 未通知资料员
**问题原因**：`send_reply.py` 使用 `MODEL:T524` 格式时，如果找不到图片，只是报错退出，没有调用资料员通知功能。之前直接发 `IMAGE:T523/...` 绕过了 MODEL 智能选图逻辑。

**修复**：在 `send_reply.py` 的 MODEL 处理逻辑中，当 `image_path` 为空时，添加资料员通知代码，调用 `send_text_safe` 通知资料员补充图片。

**修改文件**：`send_reply.py`
- 添加 `datetime` 导入
- 在找不到图片时调用 `notify_materials_staff` 逻辑

## 问题修复：智能选图发了细节图
**问题原因**：`pick_best_image()` 选了 `_01.jpg`，但这张是桌脚细节图，客户要的是整体场景图。
- `_01` 有序号加分 (+10)，但文件名没有 "detail" 关键词
- 结果选了细节图而不是场景图

**修复**：调整 `product_service.py` 中的评分逻辑
- `_02`, `_03` +15 分（通常是场景图）
- `_01` 只 +5 分（往往是细节图）
- 其他序号 +10 分

**修改文件**：`core/product_service.py` - `pick_best_image()` 函数

## 自动化：客户画像更新（15:34）
- 执行 `update_customer_profile.py`
- 处理 10/12 个客户画像
- 跳过：地下城与勇士、京东购物（广告消息）
- 重点：A畅腾升降桌09 意向度"中"，持续跟进
- 整体客户活跃度低，无高意向客户
# 2026-05-04 工作记录

## 自动化任务执行：自动更新客户画像
- **任务ID**: automation-1777517408285
- **执行时间**: 17:41
- **处理结果**:
  1. 成功更新「健康办公研究社」客户画像
  2. 追加2026-05-04产品咨询交互记录
  3. 添加"经济款需求"标签
  4. 优先级调整：低 → 中
  5. 更新备注信息

- **遇到的问题**:
  WeFlow API连接失败（消息数据库未找到），改用本地日志分析

- **下一步**:
  等待客户确认配置后进一步更新画像
